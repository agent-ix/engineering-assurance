/**
 * SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * Generated by the Agent IX TypeScript semantic backend. Do not edit.
 * backend: ix://agent-ix/filament-core-data/backend/typescript@0.1.0
 * contract: 2.0.0
 * package: agent-ix/engineering-assurance-campaign@0.0.0
 * fingerprint: sha256:7fa0a570218bdb1d90971c0826f32e7748f605b7710c40d1061392332bf9ca5c
 */

/**
 * Runtime validators, one per exported type.
 *
 * Each validator is a pure function of its argument. It reads no clock, no
 * environment variable, no file and no socket; it imports nothing outside this
 * package; it never throws, whatever it is handed; and it reports every failure
 * it finds rather than stopping at the first.
 *
 * The narrowing from `unknown` to a generated type happens in exactly one
 * construct — a user-defined type predicate — because this package contains no
 * type assertion. A `prepare` pass builds the value the caller receives, with
 * semantic defaults applied and undeclared members separated; the predicate
 * then decides that value and narrows it.
 */

import {
	VALIDATION_CODES as CODES,
	MAX_VALIDATION_DEPTH,
	copyAccessor,
	fail,
	isPlainObject,
	join,
	ownKeys,
	ownMember,
	sortErrors,
} from "./errors.js";
import type { ValidationError, ValidationResult } from "./errors.js";
import type {
	ProcedureArgumentKind,
	CampaignCompletionRule,
	CampaignVerdict,
	CampaignAttemptStatus,
	ProcedureEnvironmentKind,
	ProcedureInputOriginKind,
	MeasurementProcedure,
	ProcedureArgument,
	ProcedureArtifact,
	CampaignDefinition,
	CampaignSource,
	CampaignMember,
	CampaignRun,
	CampaignAttempt,
	CampaignRawArtifact,
	ProcedureEnvironment,
	ProcedureInputPrefix,
	ProcedureInputOrigin,
} from "./types.js";

function prepareProcedureArgumentKind(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkProcedureArgumentKind(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureArgumentKind {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"input_artifact",
		"literal",
		"output_artifact",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-001`. */
export function validateProcedureArgumentKind(
	input: unknown,
): ValidationResult<ProcedureArgumentKind> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureArgumentKind(input, 0);
	if (checkProcedureArgumentKind(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignCompletionRule(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkCampaignCompletionRule(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is CampaignCompletionRule {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["all_required"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-002`. */
export function validateCampaignCompletionRule(
	input: unknown,
): ValidationResult<CampaignCompletionRule> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignCompletionRule(input, 0);
	if (checkCampaignCompletionRule(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignVerdict(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkCampaignVerdict(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is CampaignVerdict {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["accepted", "inconclusive", "rejected"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-003`. */
export function validateCampaignVerdict(
	input: unknown,
): ValidationResult<CampaignVerdict> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignVerdict(input, 0);
	if (checkCampaignVerdict(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignAttemptStatus(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkCampaignAttemptStatus(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is CampaignAttemptStatus {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"cancelled",
		"completed",
		"containment_failure",
		"failed",
		"invalid_request",
		"malformed_response",
		"refused",
		"timed_out",
		"unavailable",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-004`. */
export function validateCampaignAttemptStatus(
	input: unknown,
): ValidationResult<CampaignAttemptStatus> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignAttemptStatus(input, 0);
	if (checkCampaignAttemptStatus(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureEnvironmentKind(
	value: unknown,
	depth: number,
): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkProcedureEnvironmentKind(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureEnvironmentKind {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = ["literal", "runtime"];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-005`. */
export function validateProcedureEnvironmentKind(
	input: unknown,
): ValidationResult<ProcedureEnvironmentKind> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureEnvironmentKind(input, 0);
	if (checkProcedureEnvironmentKind(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureInputOriginKind(
	value: unknown,
	depth: number,
): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	return value;
}

function checkProcedureInputOriginKind(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureInputOriginKind {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (typeof candidate !== "string") {
		fail(errors, pointer, CODES.NOT_A_STRING, "the value is of the wrong type");
		return false;
	}
	const variants: readonly string[] = [
		"dependency",
		"selected_bytes",
		"source_file",
	];
	if (!variants.includes(candidate)) {
		fail(
			errors,
			pointer,
			CODES.NOT_A_DECLARED_VARIANT,
			"the value names no declared variant",
		);
		return false;
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/EN-006`. */
export function validateProcedureInputOriginKind(
	input: unknown,
): ValidationResult<ProcedureInputOriginKind> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureInputOriginKind(input, 0);
	if (checkProcedureInputOriginKind(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareMeasurementProcedure(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "arguments");
		if (member.state === "value") {
			out["arguments"] = Array.isArray(member.value)
				? member.value.map((item) => prepareProcedureArgument(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "arguments");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "environment");
		if (member.state === "value") {
			out["environment"] = Array.isArray(member.value)
				? member.value.map((item) =>
						prepareProcedureEnvironment(item, depth + 1),
					)
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "environment");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "inputOrigins");
		if (member.state === "value") {
			out["inputOrigins"] = Array.isArray(member.value)
				? member.value.map((item) =>
						prepareProcedureInputOrigin(item, depth + 1),
					)
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "inputOrigins");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "inputRolePrefixes");
		if (member.state === "value") {
			out["inputRolePrefixes"] = Array.isArray(member.value)
				? member.value.map((item) =>
						prepareProcedureInputPrefix(item, depth + 1),
					)
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "inputRolePrefixes");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "inputs");
		if (member.state === "value") {
			out["inputs"] = Array.isArray(member.value)
				? member.value.map((item) => prepareProcedureArtifact(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "inputs");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "outputTrees");
		if (member.state === "value") {
			out["outputTrees"] = Array.isArray(member.value)
				? member.value.map((item) => prepareProcedureArtifact(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "outputTrees");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "outputs");
		if (member.state === "value") {
			out["outputs"] = Array.isArray(member.value)
				? member.value.map((item) => prepareProcedureArtifact(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "outputs");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "producerName");
		if (member.state === "value") {
			out["producerName"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "producerName");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "producerVersion");
		if (member.state === "value") {
			out["producerVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "producerVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "repetitions");
		if (member.state === "value") {
			out["repetitions"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "repetitions");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "responseAdapter");
		if (member.state === "value") {
			out["responseAdapter"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "responseAdapter");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "responseAdapterVersion");
		if (member.state === "value") {
			out["responseAdapterVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "responseAdapterVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "responseProtocol");
		if (member.state === "value") {
			out["responseProtocol"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "responseProtocol");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "schemaVersion");
		if (member.state === "value") {
			out["schemaVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "schemaVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceRepository");
		if (member.state === "value") {
			out["sourceRepository"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceRepository");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "timeoutMillis");
		if (member.state === "value") {
			out["timeoutMillis"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "timeoutMillis");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"arguments",
		"environment",
		"inputOrigins",
		"inputRolePrefixes",
		"inputs",
		"outputTrees",
		"outputs",
		"producerName",
		"producerVersion",
		"repetitions",
		"responseAdapter",
		"responseAdapterVersion",
		"responseProtocol",
		"schemaVersion",
		"sourceRepository",
		"timeoutMillis",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkMeasurementProcedure(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is MeasurementProcedure {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "arguments");
		const at = join(pointer, "arguments");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureArgument(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "environment");
		const at = join(pointer, "environment");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureEnvironment(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "inputOrigins");
		const at = join(pointer, "inputOrigins");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureInputOrigin(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "inputRolePrefixes");
		const at = join(pointer, "inputRolePrefixes");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureInputPrefix(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "inputs");
		const at = join(pointer, "inputs");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureArtifact(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "outputTrees");
		const at = join(pointer, "outputTrees");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureArtifact(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "outputs");
		const at = join(pointer, "outputs");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkProcedureArtifact(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "producerName");
		const at = join(pointer, "producerName");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "producerVersion");
		const at = join(pointer, "producerVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "repetitions");
		const at = join(pointer, "repetitions");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "responseAdapter");
		const at = join(pointer, "responseAdapter");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "responseAdapterVersion");
		const at = join(pointer, "responseAdapterVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "responseProtocol");
		const at = join(pointer, "responseProtocol");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "schemaVersion");
		const at = join(pointer, "schemaVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "sourceRepository");
		const at = join(pointer, "sourceRepository");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "timeoutMillis");
		const at = join(pointer, "timeoutMillis");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = [
		"arguments",
		"environment",
		"inputOrigins",
		"inputRolePrefixes",
		"inputs",
		"outputTrees",
		"outputs",
		"producerName",
		"producerVersion",
		"repetitions",
		"responseAdapter",
		"responseAdapterVersion",
		"responseProtocol",
		"schemaVersion",
		"sourceRepository",
		"timeoutMillis",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-001`. */
export function validateMeasurementProcedure(
	input: unknown,
): ValidationResult<MeasurementProcedure> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareMeasurementProcedure(input, 0);
	if (checkMeasurementProcedure(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureArgument(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "kind");
		if (member.state === "value") {
			out["kind"] =
				member.value === null
					? member.value
					: prepareProcedureArgumentKind(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "kind");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["kind", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkProcedureArgument(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureArgument {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "kind");
		const at = join(pointer, "kind");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkProcedureArgumentKind(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["kind", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-002`. */
export function validateProcedureArgument(
	input: unknown,
): ValidationResult<ProcedureArgument> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureArgument(input, 0);
	if (checkProcedureArgument(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureArtifact(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "required");
		if (member.state === "value") {
			out["required"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "required");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "role");
		if (member.state === "value") {
			out["role"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "role");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["required", "role"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkProcedureArtifact(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureArtifact {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "required");
		const at = join(pointer, "required");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "role");
		const at = join(pointer, "role");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["required", "role"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-003`. */
export function validateProcedureArtifact(
	input: unknown,
): ValidationResult<ProcedureArtifact> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureArtifact(input, 0);
	if (checkProcedureArtifact(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignDefinition(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "completionRule");
		if (member.state === "value") {
			out["completionRule"] =
				member.value === null
					? member.value
					: prepareCampaignCompletionRule(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "completionRule");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "id");
		if (member.state === "value") {
			out["id"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "id");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "members");
		if (member.state === "value") {
			out["members"] = Array.isArray(member.value)
				? member.value.map((item) => prepareCampaignMember(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "members");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "schemaVersion");
		if (member.state === "value") {
			out["schemaVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "schemaVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceGraph");
		if (member.state === "value") {
			out["sourceGraph"] = Array.isArray(member.value)
				? member.value.map((item) => prepareCampaignSource(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceGraph");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "subjectName");
		if (member.state === "value") {
			out["subjectName"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "subjectName");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "subjectVersion");
		if (member.state === "value") {
			out["subjectVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "subjectVersion");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"completionRule",
		"id",
		"members",
		"schemaVersion",
		"sourceGraph",
		"subjectName",
		"subjectVersion",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignDefinition(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is CampaignDefinition {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "completionRule");
		const at = join(pointer, "completionRule");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkCampaignCompletionRule(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "id");
		const at = join(pointer, "id");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "members");
		const at = join(pointer, "members");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				if (member.value.length < 1) {
					fail(
						errors,
						at,
						CODES.COLLECTION_TOO_SHORT,
						"the collection is shorter than its declared lower bound",
					);
				}
				for (let index = 0; index < member.value.length; index += 1) {
					checkCampaignMember(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "schemaVersion");
		const at = join(pointer, "schemaVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "sourceGraph");
		const at = join(pointer, "sourceGraph");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				if (member.value.length < 1) {
					fail(
						errors,
						at,
						CODES.COLLECTION_TOO_SHORT,
						"the collection is shorter than its declared lower bound",
					);
				}
				for (let index = 0; index < member.value.length; index += 1) {
					checkCampaignSource(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "subjectName");
		const at = join(pointer, "subjectName");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "subjectVersion");
		const at = join(pointer, "subjectVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = [
		"completionRule",
		"id",
		"members",
		"schemaVersion",
		"sourceGraph",
		"subjectName",
		"subjectVersion",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-004`. */
export function validateCampaignDefinition(
	input: unknown,
): ValidationResult<CampaignDefinition> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignDefinition(input, 0);
	if (checkCampaignDefinition(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignSource(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "digest");
		if (member.state === "value") {
			out["digest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "digest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "repository");
		if (member.state === "value") {
			out["repository"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "repository");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "revision");
		if (member.state === "value") {
			out["revision"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "revision");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["digest", "repository", "revision"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignSource(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is CampaignSource {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "digest");
		const at = join(pointer, "digest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "repository");
		const at = join(pointer, "repository");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "revision");
		const at = join(pointer, "revision");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["digest", "repository", "revision"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-005`. */
export function validateCampaignSource(
	input: unknown,
): ValidationResult<CampaignSource> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignSource(input, 0);
	if (checkCampaignSource(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignMember(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "checkerProcedure");
		if (member.state === "value") {
			out["checkerProcedure"] =
				member.value === null
					? member.value
					: prepareMeasurementProcedure(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "checkerProcedure");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "definitionVersion");
		if (member.state === "value") {
			out["definitionVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "definitionVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "dependsOn");
		if (member.state === "value") {
			out["dependsOn"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "dependsOn");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "group");
		if (member.state === "value") {
			out["group"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "group");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "name");
		if (member.state === "value") {
			out["name"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "name");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "planId");
		if (member.state === "value") {
			out["planId"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "planId");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "required");
		if (member.state === "value") {
			out["required"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "required");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"checkerProcedure",
		"definitionVersion",
		"dependsOn",
		"group",
		"name",
		"planId",
		"required",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignMember(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is CampaignMember {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "checkerProcedure");
		const at = join(pointer, "checkerProcedure");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkMeasurementProcedure(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "definitionVersion");
		const at = join(pointer, "definitionVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "dependsOn");
		const at = join(pointer, "dependsOn");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					fail(
						errors,
						join(at, String(index)),
						CODES.SHAPE_MISMATCH,
						"the element type is not declared",
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "group");
		const at = join(pointer, "group");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "name");
		const at = join(pointer, "name");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "planId");
		const at = join(pointer, "planId");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "required");
		const at = join(pointer, "required");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = [
		"checkerProcedure",
		"definitionVersion",
		"dependsOn",
		"group",
		"name",
		"planId",
		"required",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-006`. */
export function validateCampaignMember(
	input: unknown,
): ValidationResult<CampaignMember> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignMember(input, 0);
	if (checkCampaignMember(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignRun(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "attempts");
		if (member.state === "value") {
			out["attempts"] = Array.isArray(member.value)
				? member.value.map((item) => prepareCampaignAttempt(item, depth + 1))
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "attempts");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "definitionDigest");
		if (member.state === "value") {
			out["definitionDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "definitionDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "id");
		if (member.state === "value") {
			out["id"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "id");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "schemaVersion");
		if (member.state === "value") {
			out["schemaVersion"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "schemaVersion");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceGraphDigest");
		if (member.state === "value") {
			out["sourceGraphDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceGraphDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "verdict");
		if (member.state === "value") {
			out["verdict"] =
				member.value === null
					? member.value
					: prepareCampaignVerdict(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "verdict");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"attempts",
		"definitionDigest",
		"id",
		"schemaVersion",
		"sourceGraphDigest",
		"verdict",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignRun(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is CampaignRun {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "attempts");
		const at = join(pointer, "attempts");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkCampaignAttempt(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "definitionDigest");
		const at = join(pointer, "definitionDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "id");
		const at = join(pointer, "id");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "schemaVersion");
		const at = join(pointer, "schemaVersion");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "sourceGraphDigest");
		const at = join(pointer, "sourceGraphDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "verdict");
		const at = join(pointer, "verdict");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkCampaignVerdict(member.value, at, errors, surfaced, depth + 1);
		}
	}
	const declared: readonly string[] = [
		"attempts",
		"definitionDigest",
		"id",
		"schemaVersion",
		"sourceGraphDigest",
		"verdict",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-007`. */
export function validateCampaignRun(
	input: unknown,
): ValidationResult<CampaignRun> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignRun(input, 0);
	if (checkCampaignRun(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignAttempt(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "checkerRequestDigest");
		if (member.state === "value") {
			out["checkerRequestDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "checkerRequestDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "checkerResultDigest");
		if (member.state === "value") {
			out["checkerResultDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "checkerResultDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "collectionDigest");
		if (member.state === "value") {
			out["collectionDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "collectionDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "collectionId");
		if (member.state === "value") {
			out["collectionId"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "collectionId");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "domainVerdictDigest");
		if (member.state === "value") {
			out["domainVerdictDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "domainVerdictDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "index");
		if (member.state === "value") {
			out["index"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "index");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "member");
		if (member.state === "value") {
			out["member"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "member");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "rawArtifacts");
		if (member.state === "value") {
			out["rawArtifacts"] = Array.isArray(member.value)
				? member.value.map((item) =>
						prepareCampaignRawArtifact(item, depth + 1),
					)
				: member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "rawArtifacts");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "reason");
		if (member.state === "value") {
			out["reason"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "reason");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "requestDigest");
		if (member.state === "value") {
			out["requestDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "requestDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "resultDigest");
		if (member.state === "value") {
			out["resultDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "resultDigest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "status");
		if (member.state === "value") {
			out["status"] =
				member.value === null
					? member.value
					: prepareCampaignAttemptStatus(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "status");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "verdictDigest");
		if (member.state === "value") {
			out["verdictDigest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "verdictDigest");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"checkerRequestDigest",
		"checkerResultDigest",
		"collectionDigest",
		"collectionId",
		"domainVerdictDigest",
		"index",
		"member",
		"rawArtifacts",
		"reason",
		"requestDigest",
		"resultDigest",
		"status",
		"verdictDigest",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignAttempt(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is CampaignAttempt {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "checkerRequestDigest");
		const at = join(pointer, "checkerRequestDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "checkerResultDigest");
		const at = join(pointer, "checkerResultDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "collectionDigest");
		const at = join(pointer, "collectionDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "collectionId");
		const at = join(pointer, "collectionId");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "domainVerdictDigest");
		const at = join(pointer, "domainVerdictDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "index");
		const at = join(pointer, "index");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "member");
		const at = join(pointer, "member");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "rawArtifacts");
		const at = join(pointer, "rawArtifacts");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			if (!Array.isArray(member.value)) {
				fail(errors, at, CODES.NOT_AN_ARRAY, "the value is not an array");
			} else {
				for (let index = 0; index < member.value.length; index += 1) {
					checkCampaignRawArtifact(
						member.value[index],
						join(at, String(index)),
						errors,
						surfaced,
						depth + 1,
					);
				}
			}
		}
	}
	{
		const member = ownMember(candidate, "reason");
		const at = join(pointer, "reason");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "requestDigest");
		const at = join(pointer, "requestDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "resultDigest");
		const at = join(pointer, "resultDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "status");
		const at = join(pointer, "status");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkCampaignAttemptStatus(member.value, at, errors, surfaced, depth + 1);
		}
	}
	{
		const member = ownMember(candidate, "verdictDigest");
		const at = join(pointer, "verdictDigest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = [
		"checkerRequestDigest",
		"checkerResultDigest",
		"collectionDigest",
		"collectionId",
		"domainVerdictDigest",
		"index",
		"member",
		"rawArtifacts",
		"reason",
		"requestDigest",
		"resultDigest",
		"status",
		"verdictDigest",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-008`. */
export function validateCampaignAttempt(
	input: unknown,
): ValidationResult<CampaignAttempt> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignAttempt(input, 0);
	if (checkCampaignAttempt(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareCampaignRawArtifact(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "digest");
		if (member.state === "value") {
			out["digest"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "digest");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "role");
		if (member.state === "value") {
			out["role"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "role");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["digest", "role"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkCampaignRawArtifact(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is CampaignRawArtifact {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "digest");
		const at = join(pointer, "digest");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "role");
		const at = join(pointer, "role");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["digest", "role"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-009`. */
export function validateCampaignRawArtifact(
	input: unknown,
): ValidationResult<CampaignRawArtifact> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareCampaignRawArtifact(input, 0);
	if (checkCampaignRawArtifact(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureEnvironment(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "kind");
		if (member.state === "value") {
			out["kind"] =
				member.value === null
					? member.value
					: prepareProcedureEnvironmentKind(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "kind");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "name");
		if (member.state === "value") {
			out["name"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "name");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "value");
		if (member.state === "value") {
			out["value"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "value");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["kind", "name", "value"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkProcedureEnvironment(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureEnvironment {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "kind");
		const at = join(pointer, "kind");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkProcedureEnvironmentKind(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "name");
		const at = join(pointer, "name");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "value");
		const at = join(pointer, "value");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["kind", "name", "value"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-010`. */
export function validateProcedureEnvironment(
	input: unknown,
): ValidationResult<ProcedureEnvironment> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureEnvironment(input, 0);
	if (checkProcedureEnvironment(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureInputPrefix(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "prefix");
		if (member.state === "value") {
			out["prefix"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "prefix");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "required");
		if (member.state === "value") {
			out["required"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "required");
			accessor = true;
		}
	}
	const declared: readonly string[] = ["prefix", "required"];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkProcedureInputPrefix(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	_surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureInputPrefix {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "prefix");
		const at = join(pointer, "prefix");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "required");
		const at = join(pointer, "required");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = ["prefix", "required"];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-011`. */
export function validateProcedureInputPrefix(
	input: unknown,
): ValidationResult<ProcedureInputPrefix> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureInputPrefix(input, 0);
	if (checkProcedureInputPrefix(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}

function prepareProcedureInputOrigin(value: unknown, depth: number): unknown {
	if (depth > MAX_VALIDATION_DEPTH) return value;
	if (!isPlainObject(value)) return value;
	const out: Record<string, unknown> = Object.create(null);
	let accessor = false;
	{
		const member = ownMember(value, "dependencyArtifactRole");
		if (member.state === "value") {
			out["dependencyArtifactRole"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "dependencyArtifactRole");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "dependencyMember");
		if (member.state === "value") {
			out["dependencyMember"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "dependencyMember");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "kind");
		if (member.state === "value") {
			out["kind"] =
				member.value === null
					? member.value
					: prepareProcedureInputOriginKind(member.value, depth + 1);
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "kind");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "role");
		if (member.state === "value") {
			out["role"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "role");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourcePath");
		if (member.state === "value") {
			out["sourcePath"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourcePath");
			accessor = true;
		}
	}
	{
		const member = ownMember(value, "sourceRepository");
		if (member.state === "value") {
			out["sourceRepository"] = member.value;
		} else if (member.state === "accessor") {
			copyAccessor(out, value, "sourceRepository");
			accessor = true;
		}
	}
	const declared: readonly string[] = [
		"dependencyArtifactRole",
		"dependencyMember",
		"kind",
		"role",
		"sourcePath",
		"sourceRepository",
	];
	for (const key of ownKeys(value)) {
		if (declared.includes(key)) continue;
		const member = ownMember(value, key);
		if (member.state !== "value") continue;
		out[key] = member.value;
	}
	return accessor ? out : { ...out };
}

function checkProcedureInputOrigin(
	candidate: unknown,
	pointer: string,
	errors: ValidationError[],
	surfaced: ValidationError[],
	depth: number,
): candidate is ProcedureInputOrigin {
	const before = errors.length;
	if (depth > MAX_VALIDATION_DEPTH) {
		fail(
			errors,
			pointer,
			CODES.DEPTH_LIMIT_EXCEEDED,
			"the value nests past the declared bound",
		);
		return false;
	}
	if (!isPlainObject(candidate)) {
		fail(errors, pointer, CODES.NOT_AN_OBJECT, "the value is not an object");
		return false;
	}
	{
		const member = ownMember(candidate, "dependencyArtifactRole");
		const at = join(pointer, "dependencyArtifactRole");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "dependencyMember");
		const at = join(pointer, "dependencyMember");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "kind");
		const at = join(pointer, "kind");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			checkProcedureInputOriginKind(
				member.value,
				at,
				errors,
				surfaced,
				depth + 1,
			);
		}
	}
	{
		const member = ownMember(candidate, "role");
		const at = join(pointer, "role");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			fail(errors, at, CODES.MISSING_REQUIRED, "the property is absent");
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "sourcePath");
		const at = join(pointer, "sourcePath");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	{
		const member = ownMember(candidate, "sourceRepository");
		const at = join(pointer, "sourceRepository");
		if (member.state === "accessor") {
			fail(
				errors,
				at,
				CODES.ACCESSOR_MEMBER,
				"the member is an accessor and is not read",
			);
		} else if (member.state === "absent") {
			// An absent optional property is accepted.
		} else if (member.value === undefined) {
			fail(
				errors,
				at,
				CODES.EXPLICIT_UNDEFINED,
				"the property is present with the value undefined",
			);
		} else if (member.value === null) {
			fail(
				errors,
				at,
				CODES.NULL_NOT_PERMITTED,
				"the property is null and the field is not nullable",
			);
		} else {
			fail(
				errors,
				at,
				CODES.SHAPE_MISMATCH,
				"the element type is not declared",
			);
		}
	}
	const declared: readonly string[] = [
		"dependencyArtifactRole",
		"dependencyMember",
		"kind",
		"role",
		"sourcePath",
		"sourceRepository",
	];
	for (const key of ownKeys(candidate)) {
		if (declared.includes(key)) continue;
		fail(
			errors,
			join(pointer, key),
			CODES.UNKNOWN_MEMBER,
			"the member is not declared and this record rejects unknown members",
		);
	}
	return errors.length === before;
}

/** Decide an untrusted value against `ix://agent-ix/engineering-assurance-campaign/VO-012`. */
export function validateProcedureInputOrigin(
	input: unknown,
): ValidationResult<ProcedureInputOrigin> {
	const errors: ValidationError[] = [];
	const surfaced: ValidationError[] = [];
	const value = prepareProcedureInputOrigin(input, 0);
	if (checkProcedureInputOrigin(value, "", errors, surfaced, 0)) {
		return { ok: true, value, surfaced: sortErrors(surfaced) };
	}
	return { ok: false, errors: sortErrors(errors) };
}
